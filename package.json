{
  "name": "piora-obsidian",
  "version": "0.1.0",
  "private": true,
  "description": "Piora agent client for Obsidian",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.mjs",
    "package": "node scripts/package.mjs",
    "check-version": "node scripts/check-version.mjs",
    "check": "npm run test && npm run typecheck && npm run build"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "0.28.2",
    "happy-dom": "^20.14.0",
    "obsidian": "1.13.1",
    "typescript": "~5.9.3",
    "vitest": "4.1.11",
    "vite": "7.3.6"
  },
  "dependencies": {
    "diff": "^9.0.0"
  }
}
