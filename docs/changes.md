# 变更记录

按任务记录有意义的修改，最新记录在前。不得将计划执行的验证写成已经通过。

## 2026-09-13：服务端创建崩溃恢复与压力边界

- 改动：Remote 会话创建改为跨进程创建意图状态机：先落盘受保护的会话身份、JSONL 种子和请求指纹，再初始化 Agent；初始化中、授权前和授权完成后的进程退出均可用原令牌和原幂等键恢复。创建锁按幂等键分离，避免不同会话互相阻塞；同键不同参数、损坏意图/种子和归属冲突均失败关闭。
- 安全：授权前后均重新读取令牌；归属落盘在同一令牌存储锁中复核当前创建策略。种子只含会话头和受限策略标记，不含令牌、提示词或笔记正文；客户端仅识别固定错误码，不反射服务端错误正文，错误响应体最多读取 4 KiB。
- TDD：新增 12 项服务端测试，覆盖同键并发、独立键突发、授权策略竞态、损坏记录/种子、归属冲突及三个真实子进程终止边界；先以未实现恢复状态机的 Red 基线验证，再实现后全部 Green。客户端错误码白名单测试先 Red 后 Green。
- 验证：Piora 相关回归 97 项通过，tsc --noEmit 通过，npm.cmd run lint 无错误；插件 npm.cmd run check 通过，15 个测试文件 / 155 项测试、类型检查和构建通过。未运行 next build，未读取真实笔记或使用付费模型。
- 限制：真实 Next 进程的自动杀进程重启闭环仍需补到 HTTP 集成脚本；当前真实子进程测试覆盖创建状态机和本地 SDK/存储边界，不能替代真实 Obsidian 宿主验收。创建意图文件不会自动清理，强制退出残留由后续同键恢复或人工核对处理。

## 2026-09-10：本机服务发现与 Desktop Token 边界

- 改动：Piora 启动时向本用户发现目录发布仅含实例/地址/有效期的租约，每 20 秒续期、90 秒过期；插件提供显式发现、匿名身份核验和二次确认。选择只落盘地址及身份绑定，不自动连接、不迁移凭据或任务回执，不覆盖已有冲突绑定。
- 资源与安全：最多枚举 128 项、探测 16 个候选、并发 2、单次 2 秒；登记和匿名身份响应均限 4 KiB。跳过过期/损坏/身份不符记录，关闭/刷新/卸载取消扫描。默认 ~/.piora/services，可用 PIORA_DISCOVERY_DIR 统一覆盖；PIORA_DISCOVERY_DISABLED=1 可关闭服务发布。只发布实际 PORT 对应的回环地址，不扫描未登记端口。
- TDD：服务端首批 2 项租约测试及随后启动测试先 Red；插件首批 3 项读取测试、身份响应限额与 3 项选择/UI 测试先 Red，再分别实现。登记替换保护、读取限额及另 2 项 UI 用例为补充 Green 覆盖。真实 HTTP 首次因没有启动登记失败，接入 instrumentation 后通过。
- 验证：此前 npm.cmd run check 为 15 文件 / 154 项通过（43 项视图用例），类型检查/esbuild 通过；服务端相关 85 项、tsc/lint 通过。本轮 node scripts/integration.mjs 通过，隔离真实 Next 启动登记可被插件核验；启用合成 Desktop Token 后匿名身份及 Remote Bearer 调用正常、/api/health 返回 403。仍为 4 会话/6 次合成模型请求，未读取真实笔记或使用付费模型。
- 限制：发现记录与自报 serverId 不是密码学认证，不能防御恶意同用户进程或所有文件系统竞态；Windows 依赖继承 ACL，不审计 ACL。强制退出可能遗留记录，读取方不删除其他实例文件，过多残留可能触发扫描限额；旧桌面包需更新并正常重启才具有发布功能。
- 下一阶段：服务端创建与授权持久化之间的崩溃窗口、压力和真实宿主验收仍未完成；未安装到真实 Vault，未提交推送。

## 2026-09-10：图片与 UTF-8 文本附件

- 改动：添加本机文件选择、Vault 附件、输入框粘贴/拖入入口，附件列表可移除；图片为独立 images 字段，文本为不可信上下文。发送/排队/引导均使用本次快照，支持仅附件消息，回执不清空后来新增项，卸载丢弃迟到导入。
- 限制：PNG/JPEG/GIF/WebP 与 UTF-8 txt/md/csv/tsv/json/yaml/yml/log；单项 128 KiB、最多 8 项，完整 JSON（包括 Base64/转义）256 KiB。扩展名/文件头检查，Vault 真实路径边界；不自动压缩或拆分。PDF/Office/SVG/HTML 等未支持，真实原生选择器与剪贴板体验尚未验收。
- TDD：附件核心 4 项先复现空实现缺失；会话 3 项复现媒体被丢弃/无协商仍发送/排队引导缺图；Vault 路径 1 项、视图 3 项先失败后接入。补充 MIME 缺失测试先复现误接收后修复；粘贴/拖入/选择器入口 1 项补充回归直接通过，不冒充 Red。
- Piora：capabilities 声明 base64-images-v1，HTTP 先因缺声明失败；运行包装器对文本模型显式图片输入先复现未拒绝再修复。tsc 发现模型 input 可选，加入缺失能力回归复现 TypeError 后改为明确拒绝。既有消息 JSON 限额和鉴权不变。
- 验证：npm.cmd run check 为 14 文件 / 144 项通过，类型检查和 esbuild 通过；Piora Remote/运行时/队列/设置 81 项、tsc、lint 通过。node scripts/integration.mjs 最终通过：4 个合成会话、6 次合成模型调用，图片与文本真实到达视觉模型，丢回执后原 commandId 重试且无额外执行；文本模型拒绝图片且未调用模型，既有工具/队列/恢复/撤销仍通过。
- 隐私与未完成：插件配置/回执不额外保存附件内容，但已发送内容仍可能进入服务端会话历史；不承诺安全擦除。未读取真实笔记、使用付费模型、停止现有服务或运行 next build。安全本机发现、创建崩溃窗口、压力与真实宿主验收仍待完成，不提交推送。

## 2026-09-10：本地多笔记检索与显式引用

- 改动：侧栏/命令面板增加本地检索入口，按关键词匹配文件名和正文，可限制文件夹，优先使用未保存编辑器文本，结果逐项加入引用。扫描不连接 Piora、不保存查询或索引、不自动发送整库；引用时重新取完整快照，现有请求限额仍生效。
- 限额与边界：500 候选、单文件 256 KiB、读取预算 8 MiB、20 结果及 240 字符附近摘要；跳过隐藏/非法/越界/过大/不可读笔记，显示限额与跳过数量。新查询/关闭/卸载取消旧工作，摘要为纯文本。不是语义/向量检索，预算不是恶意文件系统并发下的硬隔离。
- TDD：npm.cmd test -- --run tests/note-search.test.ts 首次 4 项因空实现缺少匹配/路径范围/限额/取消而失败；实现后通过。tests/view.test.ts 新增 2 项先因缺少本地适配器/交互入口失败，接入后通过。另补 8 MiB 总预算和卸载取消 2 项验收回归，直接通过，不冒充 Red。
- 验证：npm.cmd run check 通过，13 文件 / 131 项、TypeScript 与 esbuild 均通过。检索使用合成笔记和受控宿主替身，未扫描真实 Vault。服务端代码未因检索修改；本轮此前工具阶段已完成真实 SDK/HTTP 验证（3 会话、5 合成模型请求）。
- 未完成：附件、安全本机发现、创建崩溃窗口、压力及真实宿主交互验收；不标记整个项目完成，不提交推送。

## 2026-09-10：结构化工具过程与历史对齐

- 改动：配套 Piora 声明 tool-calls-v1，正文快照投影工具开始、部分结果、成功/失败与省略数量；插件按协商和既有运行身份门禁接收，渲染可展开纯文本状态卡，按 toolCallId 合并历史，保留展开状态。无终态历史不猜成功，最终 HTTP 校准清除活动卡。
- 限额：最近 20 次活动调用，每项最多 2000 字符预览，工具名最多 100 字符，明确省略/截断；不透传参数、details、图片或 thinking。文本本身可能敏感，未声称自动脱敏。旧服务继续名称摘要。
- TDD：node --test lib/remote-content.test.mjs 首次 2 项因缺少 toolCalls 失败；npm.cmd test -- --run tests/live-state.test.ts 首次 1 项缺字段失败；控制器/视图首次 3 项因未接入状态卡失败，另 1 项复现缺少未终态历史提示。实现后全部通过。真实 HTTP 首次因缺少 toolLifecycle 能力声明失败，补实现后通过。
- 验证：npm.cmd run check 为 12 文件 / 123 项通过，类型检查和 esbuild 通过；Piora Remote/运行时/队列/设置 80 项、tsc、lint 通过。两仓库 git diff --check 通过；未运行 next build。
- 隔离 HTTP：node scripts/integration.mjs 通过；在既有 notes/创建恢复/排队/撤销覆盖后增加完整 Agent，对合成文件执行真实 SDK read 成功与失败，SSE 收到两种终态和截断结果，最终历史与活动状态对齐。共 3 个会话、5 次合成模型请求，不访问真实笔记或调用付费模型。
- 未完成：真实 Obsidian 宿主/视觉/压力验收、检索、附件、本机发现和服务端创建崩溃窗口；本批未安装到真实 Vault，未提交推送。

## 2026-09-10：会话创建回执落盘与显式恢复

- 原因：创建幂等键仅在控制器内存中，插件重启或响应丢失后可能以新键重复创建；只按服务身份恢复还不能防止换令牌导致服务端键归属改变。
- 改动：创建前保存白名单参数/原键/capabilityId，收到 sessionId 后持久化确认，最近会话选择保存成功后清理；侧栏恢复/核对入口，未知实例记录保留，完整 Agent 恢复确认，换参数/换令牌拒绝静默重试。服务端 capabilities 返回 capabilityId，校验可选 X-Piora-Capability-Id 请求头，不匹配返回 409。
- TDD：tests/session.test.ts 新增 5 项先 Red（未落盘就发 POST、落盘失败仍发送、重建控制器丢键、已确认重复发送、恢复缺少令牌归属）；tests/view.test.ts 新增 3 项先 Red（插件未恢复回执、丢其他实例记录、选择未落盘就丢回执）。服务端更换令牌断言先复现未拒绝；另 2 项先复现断开后仍发送/迟到回执重开连接，加入连接代次检查后通过。creation-storage 新增 3 项白名单/隔离/损坏数据补充回归，不冒充先前 Red。
- 验证：npm.cmd run check 为 12 文件 / 118 项通过，类型检查与 esbuild 通过。Piora Remote、运行时、队列及设置回归 78 项通过，tsc 与 lint 通过，未运行 next build。
- 真实 HTTP：node scripts/integration.mjs 注入“服务端创建成功但客户端丢回执”，断开并重建控制器，从磁盘恢复原键后返回同一 sessionId；错误 capabilityId 返回 409，确认回执在选择保存后清理。全过程恰好 2 个会话、3 次合成模型请求，既有队列、取消、消息恢复、身份与双流撤销仍通过。
- 限制：回执保存创建配置元数据（包括目录与会话名称），不保存笔记/消息或令牌；断线恢复需用户显式点击。尚未验证并消除服务端会话构造与授权幂等记录落盘之间的进程崩溃窗口，不作全局 exactly-once 承诺。真实宿主、压力、完整工具过程、检索、附件和本机发现仍待实现/验收；CLI 本次重新检查仍未启用。没有安装到真实 Vault，不提交推送。

## 2026-09-10：稳定服务身份、匿名预检与跨实例恢复隔离

- 原因：端口/地址不能唯一代表原服务，旧客户端会把凭证和仅按地址保存的回执用于后来占用同一端口的服务。
- 服务端：独立 identity.json 保存数据目录级 UUID；锁/原子写入、损坏失败，不重写 token 文件。新增匿名 /identity、capabilities/status 身份字段、设置 ID 展示，X-Piora-Server-Id 在鉴权前检查，并随长连接持续复核。
- 插件：首次显式连接对手动地址作 TOFU，先落盘绑定再鉴权；每次请求匿名核对，验证 capabilities 身份一致。实例更换有二次确认，清凭证并关闭记忆；旧/未知身份回执保留不自动恢复，任务键增加 serverId，最近会话恢复也核对实例。
- TDD：node --test lib/remote-server-identity.test.mjs 首轮 4 项 Red（缺 ID、未复用落盘值、实例不区分、错误期望头未拒绝），实现后通过；新增运行中身份变更及 UI ID 展示各 1 项先 Red 再 Green。npm.cmd run test -- tests/client.test.ts tests/task-storage.test.ts 先 4 项 Red（缺匿名探测/绑定 API、同址回执覆盖）；view 新增 4 项先 Red（绑定落盘晚于鉴权、实例变化仍连接、落盘失败仍请求、旧回执未隔离提示）；身份响应不一致及二次确认再补 2 项 Red，修复后通过。
- 验证：npm.cmd run check 为 11 文件 / 105 项通过；Piora Remote、运行时、消息路由与设置相关回归 77 项通过，类型与 lint 通过。node scripts/integration.mjs 验证匿名身份字段最小化、独立 Node 进程复用 ID、错误实例头 409、全部请求绑定后的正常会话/队列/恢复/双流撤销；模型仍恰好 3 次合成请求。未运行 next build，既有 30141/63966 服务 PID 未变化，测试服务 30142 已退出。
- 边界：serverId 不是密码学认证或进程唯一 ID，共享/复制数据目录可共享身份；不防御恶意本机 ID 伪造或探测—发送竞态。缺少 /identity 的旧 Piora 需升级源码/包，不静默弱化插件绑定；本轮不自动发现、不搬迁令牌、不安装到真实 Vault，不提交推送。
- 剩余：创建会话幂等元数据落盘、检索、附件、本机发现、完整工具过程、真实宿主和压力验收尚未完成。此前 CLI 未启用；本批次未重复宣称新的宿主检查结果。

## 2026-09-10：每令牌创建策略和生命周期 SSE 持续鉴权

- 原因：notes 会话自身受限不足以限制持有 session.create 的调用者；旧生命周期流只在连接时鉴权，撤销后的已有连接仍可能输出事件并泄漏订阅。
- 改动：服务端持久化模式/目录限制，默认 notes-only 加空根拒绝；UI 显式输入目录并默认不授权当前会话，标识 legacy 令牌。capabilities 返回每令牌限制，插件发请求前拒绝越权模式。生命周期流持续鉴权、有界缓冲、取消/abort 清理与可恢复 reset。
- TDD：创建策略 8 项此前先确认边界失败；components/RemoteControlSettings.test.mjs 新增限制契约先 1 项 Red；npm.cmd run test -- tests/session.test.ts 先复现 agent 越权请求被发送，修复后 26 项通过。node --test lib/remote-events-stream.test.mjs 首轮 6 项失败，收紧取消断言后第 7 项也复现泄漏，修复后 7 项通过；另补游标兼容和迟到状态清理 2 项回归，最终 9 项通过。
- 验证：npm.cmd run check 为 11 文件 / 95 项，类型及 esbuild 通过。Piora Remote、运行时、消息路由和设置相关回归 69 项通过；随后新增 2 项流兼容用例单独通过。服务端 tsc --noEmit 通过，lint 首轮发现 timer prefer-const，修正后通过；未运行 next build。两仓库 git diff --check 通过（插件仅有既有换行转换提示）。
- 真实 HTTP：node scripts/integration.mjs 通过；隔离 Next、测试 Vault 与合成模型验证 notes-only 拒绝 agent、目录越界/新令牌空根拒绝且没有模型调用，正常创建/排队/取消/恢复及双流撤销。全流程仍恰好 3 次合成模型请求，未触及真实笔记、既有服务或用户令牌。
- 限制：旧令牌不自动降权，需撤销重发。cwd 限制不是 Agent 文件系统沙箱，不降级已授权会话，不能撤回已发送数据或保证硬实时撤销。真实 Obsidian 验收、稳定实例身份、检索、附件、发现及完整工具过程仍未完成；本次不提交或推送。

## 2026-09-10：运行身份隔离与凭证清空语义

- 改动：增加 LiveStateGate，按会话、包装器实例、逻辑 runId 和单调序号过滤正文帧；旧连接回调、退休运行、重复及倒序帧不覆盖当前界面。HTTP 校准带本地修订栅栏，生命周期事件只触发重新读取。服务端在逻辑提示接纳时清空正文，而非每次内部 Agent 重试；包装器结束与失权分别返回可恢复 reset 和终止错误。
- 凭证：关闭记忆时通过宿主 API 清空当前连接保存值并保留内存；新增忘记令牌入口。不会跨地址/Vault 清除，不把本机忘记等同于远端撤销；宿主缺失或清空失败显式提示。旧内容协议或缺权限时显示历史同步模式。
- TDD：5 项身份门禁测试、4 项客户端竞态/降级测试、2 项内容投影测试、1 项包装器 reset 测试、1 项恢复后错误清理测试均先复现失败再实现；另有 5 项凭证/提示测试先失败再修复。包装器完整生命周期用例作为额外验收加入。测试替身的 AbortSignal 回调类型错误已修复，不将类型错误记为行为 Red。
- 验证：npm.cmd run check 通过，11 个测试文件、94 项测试，类型检查及构建通过。Piora Remote/运行时/消息路由器相关回归 48 项通过，服务端类型检查和 lint 通过。此前的排队与取消回归继续通过。
- 真实集成：node scripts/integration.mjs 通过，连续两轮返回不同合成正文，检查不同 runId 及无正文混合，仍恰好 3 次模拟模型调用；恢复不重发，已取消项不执行，正文失权后断流。未使用真实笔记或收费模型，既有 30141/63966 服务仍保持运行。
- 限制：真实 Obsidian CLI 截至本机 2026-09-10 00:04 仍未启用，宿主秘密存储/编辑器撤销等未验收；稳定服务身份、每令牌策略边界、附件、检索、发现及完整工具状态仍待实现。streamId 只标识包装器，不是 serverId。工作区改动未自动提交推送。

## 2026-09-09：排队、单条取消和多命令恢复

- 改动：SessionController 按幂等键跟踪多条命令，跨会话切换保留回执；投递前持久化、确认后逐条更新、终态逐条移除。增加 tasks 存储迁移、损坏记录报错、元数据字段白名单、明确的队列上限和排队 UI。回复快照由单个待匹配提示改为有序多提示，防止后续排队覆盖原回复的写回快照。
- 取消：Piora 增加 POST /commands/:id/cancel 和 commandCancellation 发现项，先认证，再读取命令，并按真实所属会话复核授权/撤销后调用已有路由器取消。插件只取消被选中的已跟踪命令，不把“停止当前任务”误当成清空队列。
- TDD：会话测试先复现 7 项失败，存储骨架复现 3 项行为失败，快照/队列 UI/迁移复现 4 项失败；取消接口 4 项、客户端取消及按钮各 1 项均先失败再实现。最后新增未确认投递重连测试，复现重新选会话导致重连失败，再保留原待重试状态修复。缺少存储模块的首次导入失败不计作行为 Red，随后以可运行骨架重跑行为失败。
- 验证：npm.cmd run check 通过，10 个文件、79 项测试，类型检查与构建通过。Piora 的 node --test lib/remote-*.test.mjs lib/rpc-manager-behavior.test.mjs lib/session-message-router.test.mjs 共 44 项通过；服务端 TypeScript 检查和 lint 通过。
- 集成：node scripts/integration.mjs 使用隔离的真实 Next 服务、SessionController、落盘回执及合成模型，验证排队、跨控制器重建恢复、重复取消、跨会话令牌拒绝及终态清理。模型恰好收到 3 次请求：原聊天 1 次、队列执行 2 次，被取消的第 3 条队列消息没有执行，恢复没有重发。未调用收费模型、未修改真实笔记或既有开发服务。
- 限制：宿主自动化仍待启用 CLI；完整 runId 隔离、每令牌策略边界、稳定实例身份、附件与检索等仍未完成。后台其他会话回执在重新选中该会话时校准，尚非全会话主动监控。当前更改未自动提交或推送。

## 2026-09-09：聊天输入保留与笔记交互回归

- 改动：侧栏同步右键等外部草稿变化，保持未变化输入的选区；发送/引导回执不再清空等待期间的新草稿与引用；引导按钮遵守连接、scope、运行和投递状态；初始化已打开的编辑器；设置按钮提供真实连接表单；补齐新建会话命令。
- 原因：这些缺口会使已打开侧栏的右键操作不可见、丢失下一条输入或令首次引用失败，不能仅凭核心协议测试视为可用。
- TDD：npm.cmd run test -- tests/view.test.ts 先复现 7 项目标行为失败；修复后又以缺少 new-session 命令复现 1 项失败，再补实现。额外确认取消不写入、多编辑器全部预检、预览后冲突拒绝、非活动笔记原子校验等既有行为。
- 验证：npm.cmd run check 通过，9 个文件、61 项测试，类型检查和构建通过；node scripts/integration.mjs 真实 HTTP + 合成模型闭环通过。本轮不改 Piora 源码，不运行 next build，不调用收费模型。
- 文档：新增 docs/acceptance.zh-CN.md，逐项区分已验证、间接证据与未实现要求；同步 README 操作入口。当前改动未自动提交或推送。
- 限制：宿主 CLI 仍未启用，尚未安装到用户 Vault 或完成真实 Obsidian 验收。排队、多命令恢复、runId 隔离、检索/附件、本机发现和令牌策略边界仍需继续实现。

## 2026-09-09：桌面插件原型与配套 Remote API

- 改动：新增 TypeScript/esbuild/Vitest 工具链、本机 HTTP/SSE 客户端、会话状态与幂等回执、聊天侧栏、凭证存储、笔记引用与确认写回、路径检查、构建和显式安装脚本；同步 README、设计状态与协作约定。
- 原因：将独立 Obsidian 接入方案推进为可构建原型，并由 Piora 配套 notes 策略提供服务端工具限制，而非只依赖提示词或 UI 确认。
- TDD：前序开发按功能先运行失败用例再实现；最近路径检查与安装用例的 Red 已在实现前确认。本次提交前执行 npm.cmd run check：9 个测试文件、48 项通过，类型检查与构建通过。前序 Red 命令逐次原始输出未存入仓库，本记录不将本次回归冒充重新执行的 Red。
- 配套验证：Piora 执行 node --test lib/remote-*.test.mjs lib/rpc-manager-behavior.test.mjs，32 项通过；node_modules/.bin/tsc.cmd --noEmit 与 npm.cmd run lint -- --quiet 通过。前序已用 node scripts/integration.mjs 验证真实 HTTP + 模拟模型，本次提交前未重跑该集成脚本。
- 限制：尚未在真实 Obsidian 宿主安装验收；模型调用测试使用合成夹具，不使用真实笔记或收费模型。排队、附件、检索、本机发现以及每令牌 policy/cwd 隔离尚未完成；桌面打包版需另行更新服务端，不因源码提交自动更新。

## 2026-09-09：建立 TDD 与文档同步约定

- 改动：新增根目录 `AGENTS.md`，规定 Red → Green → Refactor、行为回归测试、文档同步和交付检查；在 README 中增加协作入口。
- 原因：确保后续插件开发先验证行为，再实现，并将设计决策及验证证据保留在仓库中。
- 范围：仅新增协作规则和变更记录，不实现插件、不新增测试工具链、不改变既有 API 设计。
- 验证：UTF-8 编码、Markdown 围栏、文档相对链接与 diff 空白检查；不适用代码测试。
- 限制：当前仓库仍处于设计阶段；首次实现功能时需先建立最小测试工具链。
