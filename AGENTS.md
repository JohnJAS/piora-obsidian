# Piora for Obsidian：协作约定

本文件适用于整个仓库。开始修改前，阅读 `README.md`、`docs/design.zh-CN.md` 及目标目录内更具体的 `AGENTS.md`。保持修改聚焦，不顺带重构无关代码。

## 1. 当前阶段与架构边界

- 当前仓库处于设计阶段，没有可运行插件或测试工具链。不得把拟议能力写成已实现能力，也不得声称执行了不存在的命令。
- 插件负责交互、笔记上下文与确认写回；Piora 负责 Agent 运行。只依赖正式 Remote API，不耦合 Piora 内部组件或管理接口。
- 不把 API 令牌、工作目录、提示词或前端确认按钮当成执行沙箱。

## 2. 必须遵守 TDD

新增功能、行为变更和缺陷修复使用 Red → Green → Refactor：

1. 明确预期行为和验收条件，先写最小、可复现的失败测试。
2. 执行该测试，确认它因目标行为缺失或错误而失败，而非语法、依赖或测试环境故障。
3. 编写最小实现，使测试通过；不要先实现再补测试，冒充 TDD。
4. 在测试保持通过的前提下重构，随后运行相关回归测试及现有类型、格式等检查。

- 修复缺陷必须先加入能够复现问题的回归测试。
- 测试优先验证公开行为、输入输出和安全边界，避免绑定无关实现细节。
- 协议、状态机、幂等、重连、笔记冲突、路径校验与权限判断应有自动化测试。
- 使用受控的 HTTP、事件、时钟、文件和 Obsidian 接口替身；默认测试不调用真实模型、不消耗用户额度、不使用真实令牌或个人 Vault。
- 首次实现代码时，先建立满足该功能所需的最小测试工具链，并把准确运行命令记录到文档，不引入无关基础设施。
- 纯文档改动无需新增代码测试，检查链接、编码、结构及事实一致性即可。纯视觉调整可补充截图或人工验收，但涉及行为的部分仍须自动化测试。
- 如果受宿主或环境限制无法完成 Red/Green，先明确说明阻碍并记录可复现的验证步骤；不得谎称通过。行为变更需要偏离 TDD 时，先获得用户同意。

## 3. 改动必须同步落盘文档

- 每个任务都在 `docs/changes.md` 留下简短记录，至少说明日期、改动、原因、验证结果和已知限制；不能仅写在聊天回复中。
- 文档与实现应在同一工作批次更新；用户要求提交时一并提交，不留下“代码已改、文档以后补”。不要为了满足此规则自动提交或推送。
- 用户操作、设置、安装或运行命令变化：更新 `README.md` 或对应使用文档。
- 架构、接口契约、状态流转、安全边界或交付范围变化：更新 `docs/design.zh-CN.md` 或对应专题文档。
- 重大设计取舍应说明问题、备选方案、决策与影响；内容较多时另建 `docs/decisions/` 文档并链接，不为小改动强制创建独立决策文档。
- 纯重构或缺陷修复若不改变既有契约，只需更新变更记录，无需为了留痕无意义地改写设计方案。
- TDD 记录注明实际运行的命令、预期失败原因、通过结果和未验证项；纯文档任务明确注明“不适用代码测试”。
- 不在文档、日志、测试夹具或提交中写入令牌、凭证和用户真实笔记。示例必须使用虚构内容。

## 4. 完成前检查

- 确认只修改任务所需文件，并检查最终 diff。
- 先运行最小相关测试，再执行可用的回归测试、类型检查与格式检查；不得擅自修复无关失败。
- 确认文档、变更记录和实现状态一致；拟议、已实现、未验证应明确区分。
- 汇报修改内容、实际验证、未完成事项；未运行的检查应如实说明。
- 除非用户明确要求，不创建提交、不推送、不修改全局 Git 配置。
